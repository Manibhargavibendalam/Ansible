- name: variables from args
  hosts: local
  connection: local
  tasks:
  - name: print the course info
    ansible.builtin.debug:
      msg: "Course Name is: {{ COURSE }}, Duration: {{ DURATION }},Mentor {{ MENTOR }}"

#use the below command in terminal to run the above file and pass the variables as arguments to the playbook.
# ansible-playbook -i inventory.ini -e  ansible_user=ec2_user -e ansible_password=DevOps321 -e "COURSE=Ansible" -e "DURATION=10HRS" -e "MENTOR=vithuu reddy" 09-vars-args.yaml
