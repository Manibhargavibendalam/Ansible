- name: functions
  hosts: local
  connection: local
  vars:
    technologies: DevOps, Linux, Shell, Ansible #string
    name: "Mani Bhargavi B"
    gmail: Info@GMAIL.COM
  tasks:
  - name: default variable
    ansible.builtin.debug:
      msg: "Hello, {{ name | default ('Chintu') }}"
  #string to list
  - name: string to list
    ansible.builtin.debug:
      msg: "Technologies are {{ technologies | split(',') }}"